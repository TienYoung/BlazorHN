# HN PWA 客户端 · 前端设计说明

> 适用范围：Story 列表页、评论区展示  
> 目标平台：iOS / iPadOS（PWA，Safari WebView）  
> 设计风格：Fluent 2 为主体，HN 橙色（#FF6600）作为唯一强调色

---

## 一、整体视觉语言

### 色彩

| Token | 值 | 用途 |
|---|---|---|
| `--orange` | `#FF6600` | 唯一强调色，仅用于关键交互元素 |
| `--orange-dim` | `rgba(255,102,0,0.12)` | 强调色背景（选中态、badge） |
| `--bg` | `#f2f2ef` | 页面底色，略带暖意，非纯白 |
| `--surface` | `rgba(255,255,255,0.75)` | 卡片背景（Fluent Acrylic 质感） |
| `--surface-focus` | `#ffffff` | 当前聚焦卡片背景 |
| `--stroke` | `rgba(0,0,0,0.07)` | 卡片边框、分隔线 |
| `--t1` | `#1a1a18` | 主文字 |
| `--t2` | `#585854` | 次级文字 |
| `--t3` | `#9a9a94` | 辅助信息（时间、meta） |

Dark Mode 下，`--bg` 改为 `#0f0f0e`（暖黑），`--surface` 改为 `rgba(255,255,255,0.06)`，其余 token 相应调整。

### 字体

| 用途 | 字体 | 说明 |
|---|---|---|
| 文章标题 | Instrument Serif | 衬线，体现内容质感 |
| 评论正文 | Plus Jakarta Sans | 可读性优先 |
| meta 信息（分数、时间、域名） | Fira Code | 等宽，呼应极客气质 |

### 圆角与间距

- 卡片圆角：`12px`
- 卡片内边距：`12px 14px`
- 卡片间距：`5–8px`
- 侧边页面边距：`12px`

### 动效

- 列进入动画：左列从左侧 `translateX(-14px)` 淡入，右列从右侧淡入，中央列 `scale(0.97)` 淡入
- 动画时长：`300ms`，曲线：spring `cubic-bezier(0.34, 1.56, 0.64, 1)`
- 三列宽度变化：`grid-template-columns` transition，`360ms`，ease-out

---

## 二、Story 列表页

### 布局结构

```
┌─────────────────────────────────┐
│  顶部 Tab Bar（Top/New/Ask...）   │
├─────────────────────────────────┤
│  Story Card                     │
│  Story Card                     │
│  Story Card                     │
│  ...                            │
└─────────────────────────────────┘
```

### 顶部 Tab Bar

提供以下分类切换：`Top / New / Ask / Show / Jobs`

- 激活项：文字变为 `--t1`，下方橙色 `2px` 细线指示
- 未激活项：`--t3` 色
- Tab Bar 固定在顶部，使用 `backdrop-filter: blur` 毛玻璃效果

### Story Card

每条 Story 以卡片形式呈现，不显示图片（HN 无图）。

卡片内容从上到下：

1. **标题** — Instrument Serif，`16px`，最多 3 行截断
2. **域名** — Fira Code，`10px`，`--t3` 色
3. **meta 行** — `{分数}pts · {评论数} comments · {时间}`，Fira Code，`10px`

点击卡片后进入该 Story 的评论区视图。

---

## 三、评论区视图

评论区附属于 Story 详情页，通过点击 Story 卡片进入。页面顶部固定显示文章 TopBar，评论区在下方展开。

### 3.1 TopBar（文章标题栏）

固定在页面顶部，内容：

- 左侧：文章完整标题（Instrument Serif）+ 下方一行 meta（分数、评论数、域名）
- 右侧：chevron 图标，指示评论区展开/收起状态

**交互：点击 TopBar 整体区域，展开或收起评论区。**

| 状态 | 标题显示 | Chevron |
|---|---|---|
| 收起 | 单行截断 | 向下，`--t3` 色 |
| 展开 | 完整显示（允许换行） | 向上旋转 180°，变为橙色，背景 `--orange-dim` |

TopBar 无其他按钮，无返回按钮。

### 3.2 评论区 — 两种状态

#### 状态 A：列表视图（默认）

进入评论区后的初始状态。

- 单列全宽，展示所有**顶级评论**（即 story 的直接回复）
- 每张评论卡片完整展示（见 3.5 卡片结构）
- 可垂直滚动浏览全部顶级评论
- **点击任意评论卡片 → 进入焦点视图**

#### 状态 B：焦点视图

点击评论后触发，三列布局：

```
┌──────────┬──────────────────────┬──────────┐
│  左列    │       中列            │  右列    │
│  Parent  │  当前评论（焦点）      │  子回复  │
│ （压缩）  │  （完整阅读）          │ （压缩）  │
└──────────┴──────────────────────┴──────────┘
 ~22vw          1fr（剩余全部）        ~22vw
```

**边界情况：**

| 情形 | 布局 |
|---|---|
| 顶级评论（无 parent 评论） | 中列 + 右列，无左列 |
| leaf 评论（无子回复） | 左列 + 中列，无右列 |
| 既无 parent 又无子回复 | 仅中列全宽 |

### 3.3 左列（Parent 上下文）

- 内容：当前焦点评论的**直接父评论**，单条
- 样式：压缩模式
  - 仅显示：作者头像 + 作者名 + 时间戳 + 正文前约 80 字预览（2 行截断）
  - 隐藏：完整正文、点赞按钮、回复数 pill
  - 整列 `opacity: 0.5`，视觉退居背景
- 列头标签：`↑ {父评论作者名}`，Fira Code，`9.5px`
- **点击**：父评论成为新的焦点评论，三列重新渲染

### 3.4 中列（焦点评论）

- 当前正在阅读的评论，完整展示
- 卡片视觉强调：
  - 纯白背景 `#ffffff`
  - 橙色边框 `rgba(255,102,0,0.22)`
  - 增强阴影
  - 左侧橙色竖线装饰：`3px` 宽，上下各内缩 `14px`
- 内容完整显示，正文区域支持滚动
- 卡片本身**不可点击**（已是焦点）

### 3.5 右列（子回复列表）

- 内容：焦点评论的所有直接子回复
- 样式：与左列相同的压缩模式，`opacity: 0.5`
- 列头标签：`↓ N repl{y/ies}`，Fira Code，`9.5px`
- **点击某条回复**：该回复成为新的焦点评论，三列重新渲染

### 3.6 评论卡片结构

```
┌──────────────────────────────────────────────┐
│  [头像]  作者名                        时间   │
│                                              │
│  评论正文                                    │
│  （压缩模式：仅显示 2 行预览）                │
│                                              │
│  [▲ 412]                      [💬 3 replies] │
└──────────────────────────────────────────────┘
```

**头像**
- 尺寸：`22×22px`（压缩模式下 `18×18px`）
- 圆角：`6px`
- 内容：作者名前两个字母，全大写
- 背景色：按作者名哈希分配，共 8 种固定色值（橙/蓝/绿/紫/黄/红/青等系列）

**点赞按钮**
- 默认：`--t3` 色，透明背景
- 点击后：橙色高亮，背景 `--orange-dim`，分数显示 +1
- 再次点击：取消高亮，分数 -1

**回复数 pill**
- 显示直接子回复数量，例如 `3 replies` / `1 reply`
- 无回复时显示 `leaf`，pill `opacity: 0.35`
- pill 样式：圆角胶囊形，`--bg` 背景，`--stroke` 边框

---

## 四、导航与手势

### 核心原则

**不实现任何自定义 touch 事件。** 完全依赖 iOS 系统的原生返回手势（左滑）。

### 实现方式

使用浏览器 History API：

- 每次进入焦点评论或切换焦点时，调用 `history.pushState()` 将当前状态（当前焦点评论的 id）压入历史栈
- 用户执行 iOS 系统左滑返回手势时，触发 `popstate` 事件
- 监听 `popstate`，从事件携带的 state 中恢复上一个焦点 id，重新渲染

**状态栈示意：**

```
[列表视图]
    ↓ 点击评论 A
[评论 A 焦点]
    ↓ 点击评论 A 的回复 B
[评论 B 焦点]
    ↓ 点击回复 B 的回复 C
[评论 C 焦点]
    ↑ 左滑返回（连续可用）
```

### 无返回按钮

界面中不设置任何返回按钮。用户的返回路径为：

1. **iOS 系统左滑手势** — 逐级返回历史栈
2. **点击左列 parent 评论** — 向上跳转到父评论（同时压入新的历史记录）
3. **点击 TopBar** — 收起评论区，返回文章浏览状态

---

## 五、iPadOS 适配

屏幕宽度 ≥ 768px 时，启用 Split View 布局：

```
┌──────────────────┬──────────────────────────────┐
│                  │  TopBar（文章标题）             │
│   Story 列表     ├──────────────────────────────┤
│                  │                              │
│  （左栏 ~35%）   │  评论区（焦点视图 / 列表视图）  │
│                  │                              │
│                  │        （右栏 ~65%）           │
└──────────────────┴──────────────────────────────┘
```

- 左侧 Story 列表保持可见，选中的 Story 高亮显示
- 右侧评论区独立滚动，不影响左侧列表
- 焦点视图的侧列宽度可适当增大（建议 `~180px`，上限 `200px`）
- 两栏之间用 `1px` `--stroke` 颜色分隔线

---

## 六、PWA 相关要求

| 项目 | 要求 |
|---|---|
| `display` | `standalone`，隐藏 Safari 浏览器 UI |
| 状态栏颜色 | 与 TopBar 背景色保持一致（`#f2f2ef`） |
| 启动画面 | 橙色底（`#FF6600`）+ 居中白色 `HN` 字样 |
| Safe Area | 使用 `env(safe-area-inset-*)` 处理刘海、动态岛、Home 条 |
| 底部内边距 | 评论列表底部需留出 `env(safe-area-inset-bottom)` 空间 |
| TopBar | 使用 `position: fixed` + `backdrop-filter: blur(20px)`，滚动时保持毛玻璃效果 |
| 字体加载 | 优先使用系统字体降级方案，避免 FOUT（无样式文字闪烁） |
